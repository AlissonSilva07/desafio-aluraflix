import styled from "styled-components";

const HeroStyle = styled.section`
width: 100%;
display: flex;
flex-direction: column;
gap: 32px;
padding: 0px 120px;
`;

function Hero() {
    return (
        <section>
            
        </section>
    )
}

export default Hero;
